[project]
name = "gcp-pipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Yossi Hartman", email = "yossiyhartman@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
gcp-pipeline = "gcp_pipeline:main"

[build-system]
requires = ["uv_build>=0.9.7,<0.10.0"]
build-backend = "uv_build"
